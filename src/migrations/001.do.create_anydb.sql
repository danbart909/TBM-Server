CREATE TYPE anytype AS ENUM ('1', '2', '3');

CREATE TABLE anytable (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  param1 TEXT NOT NULL,
  param2 anytype NOT NULL,
  param3 TEXT NOT NULL
);